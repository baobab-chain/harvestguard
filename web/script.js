// The gauge-level-drop and PAYOUT trigger animations are pure CSS (see
// .gauge-fill / .trigger-ring in styles.css) so no JS is required for
// the current page. This file is a placeholder for future interactivity
// (e.g. a live testnet lookup showing current rainfall by location).
